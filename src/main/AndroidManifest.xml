<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kau.easystudio">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2">
        <activity
            android:name=".ui.IntroActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".ui.SelfEditActivity" />
        <activity android:name=".function.CropActivity" />
        <activity android:name=".function.MergeActivity" />
        <activity android:name=".ui.ImportMusicActivity" />
        <activity android:name=".ui.OneTouchEditActivity" />
        <activity android:name=".multiselectview.ShowAlbumImagesActivity" />
        <activity android:name=".ui.EditProcessActivity" />
        <activity android:name=".ui.OneTouchLastActivity"></activity>
    </application>

</manifest>